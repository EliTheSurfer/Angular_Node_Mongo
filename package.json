{
  "name": "SportsApp",
  "version": "1.0.0",
  "scripts": {
    "open-playground": "open-cli http://localhost:4200",
    "install": "npm install --prefix api && npm install --prefix client",
    "start": "concurrently \"npm start --prefix api\" \"npm start --prefix client\" \"npm run open-playground\""
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  }
}